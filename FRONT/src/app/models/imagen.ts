
// Definición de la clase Imagen
export class Imagen {
    // Propiedad que representa el ID de la imagen (puede ser de tipo string)
    id!: string;

    // Propiedad que representa el nombre de la imagen (puede ser de tipo string)
    name!: string;

    // Propiedad que representa la URL de descarga de la imagen (puede ser de tipo string)
    download_url!: string;
}
